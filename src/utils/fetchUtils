export async function weatherApi(location) {
    const res = await fetch(`https://api.openweathermap.org/data/2.5/weather?q=${location}&APPID=${process.env.API_KEY}&units=metric`).catch((err)=> {
        console.log(err)
    })
await new Promise((resolve) => setTimeout(resolve,1000))
    return res.json()
}